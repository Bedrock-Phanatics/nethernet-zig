diff --git a/src/impl/sctptransport.cpp b/src/impl/sctptransport.cpp
index 552ee94..87f7d0b 100644
--- a/src/impl/sctptransport.cpp
+++ b/src/impl/sctptransport.cpp
@@ -515,11 +515,13 @@ void SctpTransport::doRecv() {
 
 			} else {
 				// SCTP message
-				mPartialMessage.insert(mPartialMessage.end(), buffer, buffer + len);
-				if (mPartialMessage.size() > mMaxMessageSize) {
-					PLOG_WARNING << "SCTP message is too large, truncating it";
-					mPartialMessage.resize(mMaxMessageSize);
+				if (static_cast<size_t>(len) > mMaxMessageSize - mPartialMessage.size()) {
+					PLOG_WARNING << "SCTP message is too large, failing association";
+					mPartialMessage.clear();
+					changeState(State::Failed);
+					return;
 				}
+				mPartialMessage.insert(mPartialMessage.end(), buffer, buffer + len);
 
 				if (flags & MSG_EOR) {
 					// Message is complete, process it
@@ -794,8 +796,12 @@ void SctpTransport::processData(binary &&data, uint16_t sid, PayloadId ppid) {
 		break;
 
 	case PPID_STRING_PARTIAL: // deprecated
+		if (data.size() > mMaxMessageSize - mPartialStringData.size()) {
+			mPartialStringData.clear();
+			changeState(State::Failed);
+			return;
+		}
 		mPartialStringData.insert(mPartialStringData.end(), data.begin(), data.end());
-		mPartialStringData.resize(mMaxMessageSize);
 		break;
 
 	case PPID_STRING:
@@ -803,8 +809,12 @@ void SctpTransport::processData(binary &&data, uint16_t sid, PayloadId ppid) {
 			mBytesReceived += data.size();
 			recv(make_message(std::move(data), Message::String, sid));
 		} else {
+			if (data.size() > mMaxMessageSize - mPartialStringData.size()) {
+				mPartialStringData.clear();
+				changeState(State::Failed);
+				return;
+			}
 			mPartialStringData.insert(mPartialStringData.end(), data.begin(), data.end());
-			mPartialStringData.resize(mMaxMessageSize);
 			mBytesReceived += mPartialStringData.size();
 			auto message = make_message(std::move(mPartialStringData), Message::String, sid);
 			mPartialStringData.clear();
@@ -818,8 +828,12 @@ void SctpTransport::processData(binary &&data, uint16_t sid, PayloadId ppid) {
 		break;
 
 	case PPID_BINARY_PARTIAL: // deprecated
+		if (data.size() > mMaxMessageSize - mPartialBinaryData.size()) {
+			mPartialBinaryData.clear();
+			changeState(State::Failed);
+			return;
+		}
 		mPartialBinaryData.insert(mPartialBinaryData.end(), data.begin(), data.end());
-		mPartialBinaryData.resize(mMaxMessageSize);
 		break;
 
 	case PPID_BINARY:
@@ -827,8 +841,12 @@ void SctpTransport::processData(binary &&data, uint16_t sid, PayloadId ppid) {
 			mBytesReceived += data.size();
 			recv(make_message(std::move(data), Message::Binary, sid));
 		} else {
+			if (data.size() > mMaxMessageSize - mPartialBinaryData.size()) {
+				mPartialBinaryData.clear();
+				changeState(State::Failed);
+				return;
+			}
 			mPartialBinaryData.insert(mPartialBinaryData.end(), data.begin(), data.end());
-			mPartialBinaryData.resize(mMaxMessageSize);
 			mBytesReceived += mPartialBinaryData.size();
 			auto message = make_message(std::move(mPartialBinaryData), Message::Binary, sid);
 			mPartialBinaryData.clear();
diff --git a/cmake/Modules/FindMbedTLS.cmake b/cmake/Modules/FindMbedTLS.cmake
index 0b11208..bcb1f3d 100644
--- a/cmake/Modules/FindMbedTLS.cmake
+++ b/cmake/Modules/FindMbedTLS.cmake
@@ -199,8 +199,8 @@ if(MbedTLS_FOUND)
     set(MbedTLS_LIBRARIES ${MbedTLS_LIBRARY})
     set(MBEDTLS_INCLUDE_DIRS ${MbedTLS_INCLUDE_DIR})
   else()
-    set(MbedTLS_LIBRARIES ${MbedTLS_LIBRARY} ${MbedCrypto_LIBRARY} ${MbedX509_LIBRARY})
-    set_property(TARGET MbedTLS::MbedTLS PROPERTY INTERFACE_LINK_LIBRARIES MbedTLS::MbedCrypto MbedTLS::MbedX509)
+    set(MbedTLS_LIBRARIES ${MbedTLS_LIBRARY} ${MbedX509_LIBRARY} ${MbedCrypto_LIBRARY})
+    set_property(TARGET MbedTLS::MbedTLS PROPERTY INTERFACE_LINK_LIBRARIES MbedTLS::MbedX509 MbedTLS::MbedCrypto)
     set(MBEDTLS_INCLUDE_DIRS ${MbedTLS_INCLUDE_DIR})
   endif()
 endif()
